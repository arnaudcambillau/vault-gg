{% extends 'base_app.html.twig' %}

{% block title %}Ma Bibliothèque - Vault.gg{% endblock %}

{% block content %}

<!-- Messages flash -->
{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div style="padding: 16px; border-radius: 8px; margin-bottom: 16px; 
            {% if label == 'success' %}background: rgba(16, 185, 129, 0.2); border: 1px solid rgba(16, 185, 129, 0.3); color: #10B981;{% endif %}
            {% if label == 'error' %}background: rgba(239, 68, 68, 0.2); border: 1px solid rgba(239, 68, 68, 0.3); color: #EF4444;{% endif %}
            {% if label == 'warning' %}background: rgba(245, 158, 11, 0.2); border: 1px solid rgba(245, 158, 11, 0.3); color: #F59E0B;{% endif %}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}

<div style="margin-bottom: 24px;">
    <h1 style="font-size: 32px; font-weight: 900; margin-bottom: 8px;">📚 Ma Bibliothèque</h1>
    <p style="color: #94A3B8;">Gérez votre collection de jeux vidéo</p>
</div>

{% if userGames|length > 0 %}
    <!-- Grille de jeux -->
    <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 20px;">
        {% for userGame in userGames %}
            {% set game = userGame.game %}
            <div class="glass" style="padding: 16px; transition: all 0.3s ease;">
                
                <!-- Image du jeu -->
                {% if game.backgroundImage %}
                    <img 
                        src="{{ game.backgroundImage }}" 
                        alt="{{ game.name }}"
                        style="width: 100%; height: 150px; object-fit: cover; border-radius: 8px; margin-bottom: 12px;"
                    >
                {% else %}
                    <div style="width: 100%; height: 150px; background: rgba(99, 102, 241, 0.2); border-radius: 8px; display: flex; align-items: center; justify-content: center; margin-bottom: 12px;">
                        <span style="font-size: 48px;">🎮</span>
                    </div>
                {% endif %}

                <!-- Titre -->
                <h3 style="font-size: 16px; font-weight: 700; color: #F1F5F9; margin-bottom: 8px;">
                    {{ game.name }}
                </h3>

                <!-- Note et Statut -->
                <div style="display: flex; gap: 8px; margin-bottom: 8px; flex-wrap: wrap;">
                    <!-- Note -->
                    {% if game.rating %}
                        <span style="background: rgba(245, 158, 11, 0.2); color: #F59E0B; padding: 4px 8px; border-radius: 4px; font-size: 12px; font-weight: 600;">
                            ⭐ {{ game.rating }}/5
                        </span>
                    {% endif %}

                    <!-- Statut -->
                    {% if userGame.status == 'backlog' %}
                        <span style="background: rgba(107, 114, 128, 0.2); color: #9CA3AF; padding: 4px 8px; border-radius: 4px; font-size: 12px; font-weight: 600;">
                            📚 Backlog
                        </span>
                    {% elseif userGame.status == 'in_progress' %}
                        <span style="background: rgba(245, 158, 11, 0.2); color: #F59E0B; padding: 4px 8px; border-radius: 4px; font-size: 12px; font-weight: 600;">
                            🎮 En cours
                        </span>
                    {% elseif userGame.status == 'completed' %}
                        <span style="background: rgba(16, 185, 129, 0.2); color: #10B981; padding: 4px 8px; border-radius: 4px; font-size: 12px; font-weight: 600;">
                            ✅ Terminé
                        </span>
                    {% endif %}

                    <!-- Favori -->
                    {% if userGame.isFavorite %}
                        <span style="background: rgba(239, 68, 68, 0.2); color: #EF4444; padding: 4px 8px; border-radius: 4px; font-size: 12px; font-weight: 600;">
                            ❤️ Favori
                        </span>
                    {% endif %}
                </div>

                <!-- Date de sortie et ajouté le -->
                <div style="color: #94A3B8; font-size: 12px; margin-bottom: 12px;">
                    {% if game.released %}
                        <div>📅 Sortie : {{ game.released|date('Y') }}</div>
                    {% endif %}
                    <div>➕ Ajouté le {{ userGame.addedAt|date('d/m/Y') }}</div>
                </div>

                <!-- Genres -->
                {% if game.genres %}
                    <div style="margin-bottom: 12px;">
                        {% for genre in game.genres %}
                            <span style="background: rgba(99, 102, 241, 0.1); color: #6366F1; padding: 2px 6px; border-radius: 4px; font-size: 11px; margin-right: 4px; display: inline-block; margin-bottom: 4px;">
                                {{ genre }}
                            </span>
                        {% endfor %}
                    </div>
                {% endif %}

                <!-- Actions -->
<div style="display: flex; gap: 8px;">
    <!-- Changer le statut -->
    <form method="post" action="{{ path('app_home_change_status', {'id': userGame.id}) }}" style="flex: 1; margin: 0;">
        <select 
            name="status" 
            onchange="this.form.submit()"
            style="width: 100%; background: rgba(99, 102, 241, 0.2); color: #6366F1; padding: 6px; border-radius: 6px; border: 1px solid rgba(99, 102, 241, 0.3); cursor: pointer; font-size: 12px; font-weight: 600;"
        >
            <option value="">📝 Changer statut</option>
            <option value="backlog" {% if userGame.status == 'backlog' %}selected{% endif %}>📚 Backlog</option>
            <option value="in_progress" {% if userGame.status == 'in_progress' %}selected{% endif %}>🎮 En cours</option>
            <option value="completed" {% if userGame.status == 'completed' %}selected{% endif %}>✅ Terminé</option>
        </select>
    </form>
    
    <!-- Supprimer -->
    <button 
        style="flex: 1; background: rgba(239, 68, 68, 0.2); color: #EF4444; padding: 6px; border-radius: 6px; border: 1px solid rgba(239, 68, 68, 0.3); cursor: pointer; font-size: 12px; font-weight: 600;"
        onclick="alert('Supprimer - à venir')"
    >
        🗑️ Retirer
    </button>
</div>

            </div>
        {% endfor %}
    </div>
{% else %}
    <!-- Bibliothèque vide -->
    <div class="glass" style="padding: 48px; text-align: center;">
        <p style="color: #94A3B8; font-size: 18px; margin-bottom: 16px;">
            Votre bibliothèque est vide pour le moment.
        </p>
        <p style="color: #94A3B8; margin-bottom: 24px;">
            Utilisez la barre de recherche pour ajouter vos premiers jeux ! 🎮
        </p>
        <a 
            href="{{ path('app_search') }}"
            style="display: inline-block; background: linear-gradient(135deg, #6366F1 0%, #8B5CF6 100%); color: white; padding: 12px 24px; border-radius: 8px; font-weight: 600; text-decoration: none;"
        >
            🔍 Rechercher des jeux
        </a>
    </div>
{% endif %}

{% endblock %}